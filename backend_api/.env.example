# Backend API environment example (prod profile)
SPRING_PROFILES_ACTIVE=prod

# Database configuration for Postgres (prod)
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/price_db
SPRING_DATASOURCE_USERNAME=postgres
SPRING_DATASOURCE_PASSWORD=postgres

# Optional: override Hibernate/Flyway defaults
# SPRING_JPA_HIBERNATE_DDL_AUTO=validate
# SPRING_JPA_SHOW_SQL=false
# SPRING_FLYWAY_ENABLED=true
